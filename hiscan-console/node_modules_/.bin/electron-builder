#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/D:/2025-09-25 新系列/hiscan-reconstruct/node_modules/.store/electron-builder@24.13.3/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/D:/2025-09-25 新系列/hiscan-reconstruct/node_modules/.store/electron-builder@24.13.3/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../.store/electron-builder@24.13.3/node_modules/electron-builder/cli.js" "$@"
else
  exec node  "$basedir/../.store/electron-builder@24.13.3/node_modules/electron-builder/cli.js" "$@"
fi
